{
  "type": "module",
  "version": "0.4.41",
  "devDependencies": {
    "esbuild": "^0.24.0",
    "happy-dom": "^15.7.4",
    "vite": "^5.4.8",
    "vitest": "^2.1.2"
  },
  "scripts": {
    "test": "vitest --no-file-parallelism --reporter=basic --disable-console-intercept",
    "prebuild": "node update-version.js",
    "build": "node esbuild.config.js; cat dist/hide_seen_rows-header.js dist/hide_seen_rows.js | sponge dist/hide_seen_rows.js"
  }
}