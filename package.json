{
  "type": "module",
  "version": "0.4.43",
  "devDependencies": {
    "esbuild": "^0.24.0",
    "happy-dom": "^15.7.4",
    "vite": "^5.4.8",
    "vitest": "^2.1.2"
  },
  "scripts": {
    "test": "vitest --no-file-parallelism --reporter=basic --disable-console-intercept",
    "prebuild": "node update-version.js",
    "build": "node esbuild.config.js",
    "postbuild": "node post-build.js"
  }
}